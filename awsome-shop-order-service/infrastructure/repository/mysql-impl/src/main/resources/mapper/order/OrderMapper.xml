<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.awsome.shop.order.repository.mysql.mapper.order.OrderMapper">

    <!-- 按用户分页查询 -->
    <select id="pageByUserId" resultType="com.awsome.shop.order.repository.mysql.po.order.OrderPO">
        SELECT id, user_id, user_name, product_id, product_name, points_amount,
               status, created_at, updated_at, created_by, updated_by, deleted, version
        FROM redemption_order
        WHERE deleted = 0
          AND user_id = #{userId}
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        ORDER BY created_at DESC
    </select>

    <!-- 全量分页查询（管理员） -->
    <select id="pageAll" resultType="com.awsome.shop.order.repository.mysql.po.order.OrderPO">
        SELECT id, user_id, user_name, product_id, product_name, points_amount,
               status, created_at, updated_at, created_by, updated_by, deleted, version
        FROM redemption_order
        WHERE deleted = 0
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        <if test="userName != null and userName != ''">
            AND user_name LIKE CONCAT('%', #{userName}, '%')
        </if>
        ORDER BY created_at DESC
    </select>

</mapper>
