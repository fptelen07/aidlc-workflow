<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.awsome.shop.point.repository.mysql.mapper.points.PointsTransactionMapper">

    <!-- 按用户分页查询 -->
    <select id="selectPageByUserId" resultType="com.awsome.shop.point.repository.mysql.po.points.PointsTransactionPO">
        SELECT id, user_id, type, amount, reason, order_id, created_at
        FROM points_transaction
        WHERE user_id = #{userId}
        <if test="type != null and type != ''">
            AND type = #{type}
        </if>
        ORDER BY created_at DESC
    </select>

    <!-- 全量分页查询 -->
    <select id="selectPageAll" resultType="com.awsome.shop.point.repository.mysql.po.points.PointsTransactionPO">
        SELECT id, user_id, type, amount, reason, order_id, created_at
        FROM points_transaction
        <where>
            <if test="type != null and type != ''">
                AND type = #{type}
            </if>
        </where>
        ORDER BY created_at DESC
    </select>

    <!-- 按类型和月份统计总量 -->
    <select id="sumAmountByTypeAndMonth" resultType="java.lang.Long">
        SELECT COALESCE(SUM(amount), 0)
        FROM points_transaction
        WHERE type = #{type}
          AND YEAR(created_at) = #{year}
          AND MONTH(created_at) = #{month}
    </select>

</mapper>
